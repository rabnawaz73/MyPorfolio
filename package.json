{
  "name": "myporfolio",
  "version": "1.0.0",
  "description": "A portfolio website created with Flutter.",
  "main": "index.js",
  "scripts": {
    "start": "flutter run",
    "build": "flutter build web",
    "deploy": "echo \"Deploy command here\" && exit 0",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "author": "Rabnawaz",
  "license": "ISC",
  "dependencies": {
    // Add any dependencies if needed
  },
  "devDependencies": {
    // Add any devDependencies if needed
  }
}
